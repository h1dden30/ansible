---
- name: netbox compose up
  ansible.builtin.command:
    cmd: docker compose pull
    chdir: "{{ netbox_install_dir }}"
  changed_when: true

- name: netbox compose up
  ansible.builtin.command:
    cmd: docker compose up -d
    chdir: "{{ netbox_install_dir }}"
  changed_when: true
