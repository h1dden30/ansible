---
- hosts: all
  become: true
  tasks:

  - name: Update all packages
    ansible.builtin.apt:
      update_cache: yes
      upgrade: safe

  - name: Remove unused dependencies
    ansible.builtin.apt:
      autoremove: yes