---
- hosts: localhost
  become: true
  tasks:

  - name: Bootstrap server role
    ansible.builtin.include_role:
      name: servers
      tasks_from: bootstrap
    when: "'srv' in ansible_facts['nodename']"